name: Build and Release MicaPDF

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Resolve version
      run: |
        $ver = $env:GITHUB_REF -replace '^refs/tags/v',''
        if (-not $ver -or $ver -eq $env:GITHUB_REF) { $ver = '2.0.0' }
        "APP_VERSION=$ver" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
        Write-Host "Building version $ver"
      shell: pwsh
      
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'
        
    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v2
      
    - name: Restore dependencies
      run: dotnet restore
      
    - name: Build x64 Release
      run: dotnet publish -c Release -r win-x64 -p:Platform=x64 --self-contained true -p:PublishSingleFile=true -p:PublishReadyToRun=true
      
    - name: Build ARM64 Release
      run: dotnet publish -c Release -r win-arm64 -p:Platform=ARM64 --self-contained true -p:PublishSingleFile=true -p:PublishReadyToRun=true
      
    - name: Create Release Folder
      run: |
        New-Item -ItemType Directory -Force -Path "Release" | Out-Null
        Copy-Item -Path "CHANGELOG.md" -Destination "Release\CHANGELOG.md"
      shell: pwsh

    - name: Extract release notes
      run: |
        $version = $env:APP_VERSION
        $changelog = Get-Content "CHANGELOG.md" -Raw
        $pattern = "(?s)## \[$([regex]::Escape($version))\][^\#]*"
        if ($changelog -match $pattern) {
          $notes = $Matches[0].Trim()
        } else {
          $notes = "## MicaPDF $version`n`nSee CHANGELOG.md for details."
        }

        @"

        ### Downloads

        **Installer**
        - ``MicaPDF-Setup-x64.exe``
        - ``MicaPDF-Setup-ARM64.exe``

        **Portable**
        - ``MicaPDF-Portable-x64.zip``
        - ``MicaPDF-Portable-ARM64.zip``

        Release builds are self-contained (no separate .NET install).

        "@ | Out-File -FilePath "Release\RELEASE_NOTES.md" -Encoding UTF8 -NoNewline

        "`n`n" + $notes | Out-File -FilePath "Release\RELEASE_NOTES.md" -Encoding UTF8 -Append
      shell: pwsh
      
    - name: Create Installation Instructions
      run: |
        @"
        MicaPDF
        =======

        Installation

        1. Download the build for your system:
           - MicaPDF-Setup-x64.exe or MicaPDF-Portable-x64.zip
           - MicaPDF-Setup-ARM64.exe or MicaPDF-Portable-ARM64.zip

        2. Run the installer or extract the portable zip and run MicaPDF.exe.

        3. Optionally set MicaPDF as the default PDF reader when prompted.

        Basics
        - Open PDFs from the menu, recent files, or drag-and-drop
        - Edit mode: pen, highlighter, text, eraser (undo/redo supported)
        - Chapters sidebar for documents with an outline
        - Save annotated PDF writes a new PDF with your annotations
        - Settings: theme, language (EN/IT), menu layout, update check

        Requirements
        - Windows 10 version 1809 (build 17763) or later
        - Release builds are self-contained; no .NET install needed

        See CHANGELOG.md for version history.

        "@ | Out-File -FilePath "Release\INSTALL.txt" -Encoding UTF8
      shell: pwsh
      
    - name: Create README
      run: |
        @"
        # MicaPDF

        PDF viewer for Windows with Mica backdrop, zoom/navigation, and ink/text annotations.

        ## Features

        - Mica backdrop (WinUI 3)
        - Zoom, continuous scroll, double-page and cover-page modes
        - Document outline (Chapters), page labels, recent files
        - Pen, highlighter, text, eraser; undo/redo
        - Save annotated PDF, print, copy text
        - Settings: theme, language (EN/IT), menu layout
        - Drag and drop; optional default PDF association

        ## Requirements

        - Windows 10 (build 17763) or later
        - Self-contained build (no .NET install required)

        ## Download

        See the [Releases](https://github.com/TCDev69/MicaPDF/releases) page.

        ## Changelog

        See CHANGELOG.md in this folder.

        ## License

        GNU General Public License v3.0 (GPLv3). See LICENSE in the repository.

        "@ | Out-File -FilePath "Release\README.md" -Encoding UTF8
      shell: pwsh
      
    - name: Install Inno Setup
      run: |
        choco install innosetup -y
      shell: pwsh
      
    - name: Create x64 Installer Script
      run: |
        $ver = $env:APP_VERSION
        @"
        #define MyAppName "MicaPDF"
        #define MyAppVersion "$ver"
        #define MyAppPublisher "TCDev"
        #define MyAppURL "https://github.com/TCDev69/MicaPDF"
        #define MyAppExeName "MicaPDF.exe"

        [Setup]
        AppId={{A1B2C3D4-E5F6-7890-ABCD-EF1234567890}
        AppName={#MyAppName}
        AppVersion={#MyAppVersion}
        AppPublisher={#MyAppPublisher}
        AppPublisherURL={#MyAppURL}
        AppSupportURL={#MyAppURL}
        AppUpdatesURL={#MyAppURL}
        DefaultDirName={autopf}\{#MyAppName}
        DefaultGroupName={#MyAppName}
        AllowNoIcons=yes
        OutputDir=Release
        OutputBaseFilename=MicaPDF-Setup-x64
        Compression=lzma2
        SolidCompression=yes
        WizardStyle=modern
        ArchitecturesAllowed=x64
        ArchitecturesInstallIn64BitMode=x64
        PrivilegesRequired=lowest

        [Languages]
        Name: "english"; MessagesFile: "compiler:Default.isl"

        [Tasks]
        Name: "desktopicon"; Description: "{cm:CreateDesktopIcon}"; GroupDescription: "{cm:AdditionalIcons}"; Flags: unchecked

        [Files]
        Source: "bin\x64\Release\net10.0-windows10.0.22621.0\win-x64\publish\MicaPDF.exe"; DestDir: "{app}"; Flags: ignoreversion

        [Icons]
        Name: "{group}\{#MyAppName}"; Filename: "{app}\{#MyAppExeName}"
        Name: "{group}\{cm:UninstallProgram,{#MyAppName}}"; Filename: "{uninstallexe}"
        Name: "{autodesktop}\{#MyAppName}"; Filename: "{app}\{#MyAppExeName}"; Tasks: desktopicon

        [Run]
        Filename: "{app}\{#MyAppExeName}"; Description: "{cm:LaunchProgram,{#StringChange(MyAppName, '&', '&&')}}"; Flags: nowait postinstall skipifsilent

        [Registry]
        Root: HKCU; Subkey: "Software\Classes\.pdf"; ValueType: string; ValueName: ""; ValueData: "MicaPDF.Document"; Flags: uninsdeletevalue
        Root: HKCU; Subkey: "Software\Classes\MicaPDF.Document"; ValueType: string; ValueName: ""; ValueData: "PDF Document"; Flags: uninsdeletekey
        Root: HKCU; Subkey: "Software\Classes\MicaPDF.Document\DefaultIcon"; ValueType: string; ValueName: ""; ValueData: "{app}\{#MyAppExeName},0"
        Root: HKCU; Subkey: "Software\Classes\MicaPDF.Document\shell\open\command"; ValueType: string; ValueName: ""; ValueData: """{app}\{#MyAppExeName}"" ""%1"""
        "@ | Out-File -FilePath "installer-x64.iss" -Encoding UTF8
      shell: pwsh
      
    - name: Create ARM64 Installer Script
      run: |
        $ver = $env:APP_VERSION
        @"
        #define MyAppName "MicaPDF"
        #define MyAppVersion "$ver"
        #define MyAppPublisher "TCDev"
        #define MyAppURL "https://github.com/TCDev69/MicaPDF"
        #define MyAppExeName "MicaPDF.exe"

        [Setup]
        AppId={{A1B2C3D4-E5F6-7890-ABCD-EF1234567890}
        AppName={#MyAppName}
        AppVersion={#MyAppVersion}
        AppPublisher={#MyAppPublisher}
        AppPublisherURL={#MyAppURL}
        AppSupportURL={#MyAppURL}
        AppUpdatesURL={#MyAppURL}
        DefaultDirName={autopf}\{#MyAppName}
        DefaultGroupName={#MyAppName}
        AllowNoIcons=yes
        OutputDir=Release
        OutputBaseFilename=MicaPDF-Setup-ARM64
        Compression=lzma2
        SolidCompression=yes
        WizardStyle=modern
        ArchitecturesAllowed=arm64
        ArchitecturesInstallIn64BitMode=arm64
        PrivilegesRequired=lowest

        [Languages]
        Name: "english"; MessagesFile: "compiler:Default.isl"

        [Tasks]
        Name: "desktopicon"; Description: "{cm:CreateDesktopIcon}"; GroupDescription: "{cm:AdditionalIcons}"; Flags: unchecked

        [Files]
        Source: "bin\ARM64\Release\net10.0-windows10.0.22621.0\win-arm64\publish\MicaPDF.exe"; DestDir: "{app}"; Flags: ignoreversion

        [Icons]
        Name: "{group}\{#MyAppName}"; Filename: "{app}\{#MyAppExeName}"
        Name: "{group}\{cm:UninstallProgram,{#MyAppName}}"; Filename: "{uninstallexe}"
        Name: "{autodesktop}\{#MyAppName}"; Filename: "{app}\{#MyAppExeName}"; Tasks: desktopicon

        [Run]
        Filename: "{app}\{#MyAppExeName}"; Description: "{cm:LaunchProgram,{#StringChange(MyAppName, '&', '&&')}}"; Flags: nowait postinstall skipifsilent

        [Registry]
        Root: HKCU; Subkey: "Software\Classes\.pdf"; ValueType: string; ValueName: ""; ValueData: "MicaPDF.Document"; Flags: uninsdeletevalue
        Root: HKCU; Subkey: "Software\Classes\MicaPDF.Document"; ValueType: string; ValueName: ""; ValueData: "PDF Document"; Flags: uninsdeletekey
        Root: HKCU; Subkey: "Software\Classes\MicaPDF.Document\DefaultIcon"; ValueType: string; ValueName: ""; ValueData: "{app}\{#MyAppExeName},0"
        Root: HKCU; Subkey: "Software\Classes\MicaPDF.Document\shell\open\command"; ValueType: string; ValueName: ""; ValueData: """{app}\{#MyAppExeName}"" ""%1"""
        "@ | Out-File -FilePath "installer-arm64.iss" -Encoding UTF8
      shell: pwsh
      
    - name: Build Installers
      run: |
        & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer-x64.iss
        & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer-arm64.iss
      shell: pwsh
      continue-on-error: true
      
    - name: Create Portable ZIP
      run: |
        $docs = @(
          "Release\INSTALL.txt",
          "Release\README.md",
          "Release\CHANGELOG.md"
        )
        Compress-Archive -Path @(
          "bin\x64\Release\net10.0-windows10.0.22621.0\win-x64\publish\MicaPDF.exe"
        ) + $docs -DestinationPath "Release\MicaPDF-Portable-x64.zip" -Force
        Compress-Archive -Path @(
          "bin\ARM64\Release\net10.0-windows10.0.22621.0\win-arm64\publish\MicaPDF.exe"
        ) + $docs -DestinationPath "Release\MicaPDF-Portable-ARM64.zip" -Force
      shell: pwsh
      
    - name: Upload Release Assets
      uses: actions/upload-artifact@v4
      with:
        name: MicaPDF-Release
        path: |
          Release/*.exe
          Release/*.zip
          Release/*.txt
          Release/*.md
          
    - name: Create GitHub Release
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v2
      with:
        files: |
          Release/MicaPDF-Portable-x64.zip
          Release/MicaPDF-Portable-ARM64.zip
          Release/MicaPDF-Setup-x64.exe
          Release/MicaPDF-Setup-ARM64.exe
          Release/INSTALL.txt
          Release/README.md
          Release/CHANGELOG.md
        body_path: Release/RELEASE_NOTES.md
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
